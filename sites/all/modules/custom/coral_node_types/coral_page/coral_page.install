<?php
  
/**
 * Implements hook_uninstall().
 */
function coral_page_uninstall() {      
  // Delete nodes and node types.
  $type   = 'page';
  $result = db_select('node', 'n')
        ->fields('n', array('nid'))
        ->condition('type', $type, '=')
        ->execute();

  foreach ($result as $record) {
    node_delete($record->nid);
  }
  node_type_delete($type);
}
